server {
    listen 80;
    server_name eddva.in www.eddva.in *.eddva.in;
    root /var/www/eddva_frontend;
    index index.html;

    # ── index.html: NEVER cache ───────────────────────────────────────────────
    location = /index.html {
        add_header Cache-Control "no-cache, no-store, must-revalidate";
        add_header Pragma        "no-cache";
        add_header Expires       "0";
        try_files $uri =404;
    }

    # ── /assets/: cache forever (Vite content-hashes every filename) ─────────
    location /assets/ {
        add_header Cache-Control "public, max-age=31536000, immutable";
        try_files $uri =404;
    }

    # ── SPA fallback: all other routes serve index.html ───────────────────────
    location / {
        try_files $uri $uri/ /index.html;
        add_header Cache-Control "no-cache";
    }

    # ── Gzip ─────────────────────────────────────────────────────────────────
    gzip on;
    gzip_types text/plain text/css application/javascript application/json image/svg+xml;
    gzip_min_length 1024;
}

# ── HTTPS (if SSL is configured via certbot / wildcard cert) ─────────────────
# Uncomment and fill in the cert paths once you have a wildcard cert for *.eddva.in
#
# server {
#     listen 443 ssl http2;
#     server_name eddva.in www.eddva.in *.eddva.in;
#     root /var/www/eddva_frontend;
#     index index.html;
#
#     ssl_certificate     /etc/letsencrypt/live/eddva.in/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/eddva.in/privkey.pem;
#     ssl_protocols       TLSv1.2 TLSv1.3;
#     ssl_ciphers         HIGH:!aNULL:!MD5;
#
#     location = /index.html {
#         add_header Cache-Control "no-cache, no-store, must-revalidate";
#         add_header Pragma        "no-cache";
#         add_header Expires       "0";
#         try_files $uri =404;
#     }
#
#     location /assets/ {
#         add_header Cache-Control "public, max-age=31536000, immutable";
#         try_files $uri =404;
#     }
#
#     location / {
#         try_files $uri $uri/ /index.html;
#         add_header Cache-Control "no-cache";
#     }
#
#     gzip on;
#     gzip_types text/plain text/css application/javascript application/json image/svg+xml;
#     gzip_min_length 1024;
# }
#
# server {
#     listen 80;
#     server_name eddva.in www.eddva.in *.eddva.in;
#     return 301 https://$host$request_uri;
# }
